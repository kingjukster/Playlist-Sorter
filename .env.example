# Private library access requires tokens created through Apple's documented MusicKit flow.
# Never commit real values. The CLI reads these only from the process environment.
APPLE_MUSIC_DEVELOPER_TOKEN=
APPLE_MUSIC_USER_TOKEN=
